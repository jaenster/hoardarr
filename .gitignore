# Build output
/hoardarr
/testserver-nntpd
/dist/
/bin/

# Frontend
/frontend/node_modules/
/frontend/dist/
/frontend/.vite/
/frontend/*.tsbuildinfo
/frontend/test-results/
/frontend/playwright-report/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Runtime / data
/data/
/tmp/
*.db
*.db-journal
*.log
*.nzb

# Local config — config.toml is auto-generated on first run with
# an issued API key; never commit it.
/config.toml
config.local.yaml
.env
.env.local
.claude/

# Local diagnostic fixtures pulled from production. Real PAR2/RAR
# bytes — keep out of the public repo. repair_obfuscated_test.go
# skips gracefully when these are absent; copy from the live host
# (e.g. `scp opslagbeest:/path/to/file.tmp testdata/...`) to repro.
/testdata/repair-bug-job38/
